import Vue from 'vue';
import Vuex from 'vuex';
import { marketInfo } from './marketInfo';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    marketInfo
  }
});
